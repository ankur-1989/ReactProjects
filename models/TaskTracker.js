class Task {
    constructor(ownerId, date, task, hours, minutes) {
        this.ownerId = ownerId;
        this.date = date;
        this.task = task;
        this.hours = hours;
        this.minutes = minutes;
    }
}

export default Task;