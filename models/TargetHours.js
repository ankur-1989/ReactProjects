class TargetHour {
    constructor(ownerId, date, targetHour, hoursSpent) {
        this.ownerId = ownerId;
        this.date = date;
        this.targetHour = targetHour;
        this.hoursSpent = hoursSpent;
    }
}

export default TargetHour;